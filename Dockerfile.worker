FROM python:3.11-slim

WORKDIR /app

COPY requirements-worker.txt .
RUN pip install --no-cache-dir -r requirements-worker.txt

COPY k8s_ops.py worker.py ./

ENV PORT=8080
CMD ["sh", "-c", "uvicorn worker:app --host 0.0.0.0 --port ${PORT}"]
